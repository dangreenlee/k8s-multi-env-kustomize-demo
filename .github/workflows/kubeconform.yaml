name: Validate Kubernetes Manifests

on:
  pull_request:
    branches: [ main ]

jobs:
  kubeconform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: yokawasa/action-setup-kube-tools@v0.9.2
      - uses: fairwindsops/polaris/.github/actions/setup-polaris@master
        with:
          setup-tools: |
            kubeconform
            kustomize
            polaris
          kubeconform: '0.5.0'
          kustomize: '4.5.7'
          polaris: '5.0.0'
      - run: |
          kustomize build apps/my-web-app/overlays/environments/prod -o prod_consolidated_manifest.yaml
          polaris audit --audit-path ./prod_consolidated_manifest.yaml --format=pretty --set-exit-code-on-danger --set-exit-code-below-score 80
          kustomize build apps/my-web-app/overlays/environments/prod | kubeconform -verbose
          kustomize build apps/my-web-app/overlays/environments/stage | kubeconform -verbose
          
